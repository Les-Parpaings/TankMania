
#include "tank.hpp"

//Load all fonts of the game
void get_all_fonts(all_fonts_t &fonts)
{
    fonts.hud.loadFromFile(FONT_HUD);
}

//Load all textures of the game
void get_all_textures(all_textures_t &textures)
{
    // textures.background.loadFromFile(IMAGE_BACKGROUND);
}
